<article class="columns eight alpha">
	<% if defined?(image) && image %>
	  <%= image_tag image.attachment.url(:product), :itemprop => "image" %>
	<% else %>
	  <%= rbc_product_image(@product, :itemprop => "image") %>
	<% end %>
</article>